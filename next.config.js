/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [
      "i.pinimg.com",
      "images.pexels.com",
      "links.papareact.com",
      ],
  },
}

module.exports = nextConfig
